import { DoDebounceDirective } from './do-debounce.directive';

describe('DoDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new DoDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
